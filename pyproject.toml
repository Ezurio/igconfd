[build-system]
build-backend = "setuptools.build_meta"
requires = ["setuptools"]

[project]
name = "igconfd"
version = "1.0"
description = "Industrial Gateway igconfd"
license = { file = "LICENSE.ezurio" }
